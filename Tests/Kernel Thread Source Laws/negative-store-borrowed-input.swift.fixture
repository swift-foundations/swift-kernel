// Expected rejection: a ~Escapable borrowed input cannot be stored in a class.
import Kernel

struct Input: ~Copyable, ~Escapable, Sendable {}

final class Storage {
    var input: Input?
}

func store(_ input: borrowing Input, in storage: Storage) {
    storage.input = input
}
